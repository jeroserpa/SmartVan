// System headers the van-core-probes.yaml lambdas use. ESPHome's `includes:`
// takes local files only, so this file exists to pull them in.
#pragma once
#include <esp_heap_caps.h>
#include <esp_wifi.h>
